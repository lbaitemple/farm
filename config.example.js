// Copy this file to config.js and fill in your own values for local development.
// config.js is gitignored — it is never committed. On GitHub Pages, config.js is
// generated at deploy time from the CLOUD_URL / CLOUD_TOKEN repo secrets
// (see .github/workflows/deploy-pages.yml).
window.CLOUD_CONFIG = {
  url: "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec",
  token: "your-write-token",
  gameId: "qqfarm_default"
};
